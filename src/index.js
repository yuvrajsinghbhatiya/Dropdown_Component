import React from 'react';  //This imports the React module, which is necessary for creating React components and using JSX syntax.

import ReactDOM from 'react-dom';  // This imports the ReactDOM module, which is necessary for rendering React components to the DOM.

import App from './App';  // This imports the App component, which is the root component of the application.




ReactDOM.render(<App />, document.getElementById('root')); //This renders the App component to the DOM element with the id of root.
